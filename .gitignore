__pycache__/
*.pyc
.venv/
.streamlit/secrets.toml
.vscode/

# Runtime data, not source. Once the app is deployed, Streamlit Cloud
# rebuilds the container from this repo on every push - if these stayed
# tracked, pushing a local snapshot would overwrite whatever real users
# have logged on the live instance since the last deploy.
climber_profiles/
