.git
.venv
.vscode
.devcontainer
__pycache__
*.pyc

# Credentials - mounted at runtime by docker-compose, never baked into the image
.streamlit/secrets.toml
secrets.toml

# Runtime data - lives in a mounted volume, not the image
climber_profiles/

# GitHub Pages site (home page + privacy policy), not part of the app
docs/
